import 'package:flutter/material.dart';

class SearchPaqeContent extends StatelessWidget {
  const SearchPaqeContent({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('trzy'),
    );
  }
}
