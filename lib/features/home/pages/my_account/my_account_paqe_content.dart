import 'package:flutter/material.dart';

class MyAccountPaqeContent extends StatelessWidget {
  const MyAccountPaqeContent({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('dwa'),
    );
  }
}
